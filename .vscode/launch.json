{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "Go: Main",
      "type": "go",
      "request": "launch",
      "mode": "auto",
      "program": "${workspaceFolder}/main.go",
      "env": {
        "PORT": "8080"//,
        //"DATABASE_URL":"postgres://jvcte@localhost:5432/glossias?sslmode=disable"
      }
    }
  ]
}
