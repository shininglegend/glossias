meta {
  name: Check Vocab
  type: http
  seq: 5
}

post {
  url: {{baseURL}}/api/stories/{{adminStoryId}}/check-vocab
  body: json
  auth: inherit
}

body:json {
  {
    "vocab_key": "5-2",
    "answers": "word1"
  }
}

settings {
  encodeUrl: true
}
