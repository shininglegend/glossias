meta {
  name: Add Course Admin
  type: http
  seq: 7
}

post {
  url: {{baseURL}}/api/admin/courses/{{courseId}}/admins
  body: json
  auth: inherit
}

body:json {
  {
    "email": "user@example.com"
  }
}

settings {
  encodeUrl: true
}
