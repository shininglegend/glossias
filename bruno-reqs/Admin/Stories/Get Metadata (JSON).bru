meta {
  name: Get Metadata (JSON)
  type: http
  seq: 3
}

get {
  url: {{baseURL}}/admin/stories/{{adminStoryId}}/metadata
  headers: {
    Accept: application/json
  }
  auth: inherit
}

settings {
  encodeUrl: true
}

