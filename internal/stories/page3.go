package stories

import "net/http"

func (h *Handler) ServePage3(w http.ResponseWriter, r *http.Request) {
	// TODO: Return a page showing the grammar points on that page.
	// A future edit will have them select the grammar points, but for now just underline them
}
